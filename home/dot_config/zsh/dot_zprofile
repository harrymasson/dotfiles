# ~/.config/zsh/.zprofile
# Purpose: Login shell setup — runs once on login.
# Source: chezmoi: home/dot_config/zsh/dot_zprofile
# Notes: Homebrew shellenv sets PATH, MANPATH, INFOPATH, HOMEBREW_*.

eval "$(/opt/homebrew/bin/brew shellenv)"
